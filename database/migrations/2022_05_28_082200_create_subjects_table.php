<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateSubjectsTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('subjects', function (Blueprint $table) {
            $table->id();
            $table->string('certificate_code');
            $table->decimal('economics');
            $table->decimal('islamic_studies');
            $table->decimal('mathemetics');
            $table->decimal('agriculture_science');
            $table->decimal('biology');
            $table->decimal('chemistry');
            $table->decimal('physics');
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('subjects');
    }
}
